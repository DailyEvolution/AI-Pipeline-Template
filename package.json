{
  "name": "pipeline-template",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:contract": "vitest run tests/contract",
    "selftest": "bash scripts/selftest.sh"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
